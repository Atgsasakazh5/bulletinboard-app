# ===================================================================
# Spring Boot アプリケーション設定ファイル（サンプル）
# ===================================================================
#
# このファイルは設定ファイルのサンプルです。
# ローカルでアプリケーションを実行するには、このファイルを "application.properties" という名前にコピーし、
# 必要な秘密情報（秘密鍵など）を記入してください。
# "application.properties" ファイルは .gitignore によってGitの管理対象外となっており、コミットすべきではありません。
#

# --- アプリケーション名 ---
spring.application.name=bulletinboard-app

# ===================================================================
# --- H2データベース設定（ローカル開発用） ---
# ===================================================================
# H2のWebコンソールを /h2-console で有効にする
spring.h2.console.enabled=true

# インメモリデータベースの接続URL
spring.datasource.url=jdbc:h2:mem:testdb
spring.datasource.username=sa
spring.datasource.password=

# ===================================================================
# --- JPA/Hibernate 設定（ローカル開発用） ---
# ===================================================================
# 起動のたびに、エンティティを元にデータベーススキーマを削除・再作成する。
# 開発中に毎回クリーンな状態で始めるのに適しています。
spring.jpa.hibernate.ddl-auto=create-drop

# ===================================================================
# --- JWT 設定 ---
# ===================================================================
# あなた自身の秘密鍵を設定する必要があります。
# HS512アルゴリズムのため、64バイト以上のBase64エンコードされた文字列であるべきです。
# `openssl rand -base64 64` のようなコマンドで生成できます。
app.jwtSecret=ここにあなた自身のBase64エンコード済み秘密鍵を設定してください

# JWTの有効期限（ミリ秒単位）。デフォルト: 86400000ms = 24時間
app.jwtExpirationMs=86400000